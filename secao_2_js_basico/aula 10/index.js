const nome = 'Luiz';
const nome1 = "Luiz";
const nome2 = `Luiz`; // string

const num = 10; //number
const num1 = 10.52 //number
let nomeAluno; // underfined -> não aponta pra nada na memória
let sobrenomeAluno = null; //nulo -> não aponta pra nada na memória mas é diferente do underfined

const boolean = true; // boolean = true ou false (Lógico)
const aprovado = false;

console.log(typeof aprovado, aprovado);

 